name: Python package

on: push

jobs:
  build:

    runs-on: self-hosted
    strategy:
      fail-fast: false

    steps:

    - name: Checkout
      uses: actions/checkout@v3

    - name: Run tests
      run: pytest
      
        
